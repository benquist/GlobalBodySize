% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_app.R
\name{run_app}
\alias{run_app}
\title{Launch the California Poppy SDM Shiny Application}
\usage{
run_app(worldclim_path = NULL, launch.browser = TRUE, ...)
}
\arguments{
\item{worldclim_path}{Character. Directory where WorldClim rasters will be
downloaded / reused.  When \code{NULL} (default) the function first checks for
a \verb{worldclim_data/} sub-directory in the current working directory (which
is present if you have already run the companion Rmd), then falls back to
a temporary directory.}

\item{launch.browser}{Logical. Open the default browser automatically?
Passed to \code{\link[shiny:runApp]{shiny::runApp()}}. Default \code{TRUE}.}

\item{...}{Additional arguments forwarded to \code{\link[shiny:runApp]{shiny::runApp()}}.}
}
\value{
Called for its side-effect (launches a Shiny app).
}
\description{
Opens an interactive Shiny dashboard for fitting and exploring a MaxEnt
Species Distribution Model for \emph{Eschscholzia californica} (California
Poppy).  The app fetches BIEN occurrence data, loads WorldClim v2.1
bioclimatic variables, trains a \code{maxnet} model, and visualises predicted
habitat suitability on an interactive leaflet map with ROC curves, response
curves, permutation importance, and a diagnostics table.
}
\examples{
\dontrun{
  # Re-use WorldClim data already downloaded alongside the companion Rmd
  run_app(worldclim_path = file.path(getwd(), "worldclim_data"))

  # Let the app download data to a temporary directory
  run_app()
}

}
