# ── Orphan shapefiles generated by BIEN_ranges_sf ──────────────────────────
# These are temporary range downloads written to the working directory.
# They are regenerated on each run and must NOT be committed.
app/*.shp
app/*.dbf
app/*.prj
app/*.shx
app/*.shx

# Any shapefiles anywhere in the repo
*.shp
*.dbf
*.prj
*.shx

# ── Large spatial data files ─────────────────────────────────────────────────
data/
worldclim_data/
*.tif
*.geotiff

# ── R artifacts ─────────────────────────────────────────────────────────────
*.Rcheck/
*.tar.gz
*.Rproj.user/
.Rhistory
.RData
.Ruserdata

# ── shinyapps.io deployment cache ───────────────────────────────────────────
rsconnect/

# ── OS and editor noise ──────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
